# 🚀 陶瓷文化模块快速启动指南

## ⚡ 3分钟快速部署

### 步骤1：导入测试数据 (1分钟)

打开终端，执行以下命令：

```bash
cd /Users/shirenan/Desktop/Zsq_JingdezhenCulturalTourismServicePlatform/backend/src/main/resources

# 导入测试数据
mysql -u root -p jingdezhen_tourism < ceramic_content_test_data.sql
```

输入MySQL密码后，等待导入完成。

**快速验证：**
```sql
mysql -u root -p -e "USE jingdezhen_tourism; SELECT COUNT(*) FROM ceramic_content;"
```

应该显示10条数据。

---

### 步骤2：启动后端 (1分钟)

**方式1：使用IDEA（推荐）**
1. 打开IDEA
2. 找到 `TourismApplication.java`
3. 点击绿色运行按钮

**方式2：使用命令行**
```bash
cd /Users/shirenan/Desktop/Zsq_JingdezhenCulturalTourismServicePlatform/backend
mvn spring-boot:run
```

**验证后端启动成功：**
访问浏览器：http://localhost:8080/api/ceramic/list

应该看到JSON格式的陶瓷文化数据。

---

### 步骤3：启动前端 (1分钟)

打开新的终端窗口：

```bash
cd /Users/shirenan/Desktop/Zsq_JingdezhenCulturalTourismServicePlatform/frontend

# 启动开发服务器
npm run dev
```

**验证前端启动成功：**
访问浏览器：http://localhost:5173/ceramic

应该看到陶瓷文化页面，显示10条内容。

---

## ✅ 功能测试清单

打开 http://localhost:5173/ceramic，依次测试：

- [ ] **1. 列表展示** - 页面显示陶瓷文化内容卡片
- [ ] **2. 分类筛选** - 点击不同分类标签（全部、陶瓷历史、制作工艺等）
- [ ] **3. 详情查看** - 点击任意卡片，弹出详情对话框
- [ ] **4. 浏览量统计** - 查看详情后，关闭对话框，浏览量应该+1
- [ ] **5. 分页功能** - 切换页码，查看不同页的内容

---

## 📁 已创建的文件

### 后端文件 (4个)
```
backend/src/main/java/com/jingdezhen/tourism/
├── mapper/CeramicContentMapper.java          ✅ 数据访问层
├── service/CeramicContentService.java        ✅ 服务接口
├── service/impl/CeramicContentServiceImpl.java ✅ 服务实现
└── controller/CeramicContentController.java  ✅ 控制器

backend/src/main/resources/
└── ceramic_content_test_data.sql             ✅ 测试数据
```

### 前端文件 (2个)
```
frontend/src/
├── api/ceramic.js                            ✅ API接口
└── views/Ceramic.vue                         ✅ 页面（已改造）
```

---

## 🎯 API接口总览

| 方法 | 路径 | 功能 |
|------|------|------|
| GET | `/api/ceramic/list` | 获取列表（支持分页和分类筛选） |
| GET | `/api/ceramic/{id}` | 获取详情 |
| POST | `/api/ceramic/{id}/view` | 增加浏览量 |
| GET | `/api/ceramic/categories` | 获取分类统计 |

---

## 🐛 遇到问题？

### ❌ 数据库连接失败
```bash
# 检查MySQL是否启动
mysql -u root -p -e "SELECT 1"

# 检查数据库是否存在
mysql -u root -p -e "SHOW DATABASES LIKE 'jingdezhen_tourism'"
```

### ❌ 后端启动失败
检查端口8080是否被占用：
```bash
lsof -i:8080
```

### ❌ 前端显示空白
1. 打开浏览器开发者工具 (F12)
2. 查看Console标签，检查是否有错误
3. 查看Network标签，检查API请求是否成功

### ❌ 接口404错误
确认：
- 后端已启动
- 访问路径包含 `/api` 前缀
- Controller路径为 `/ceramic`

---

## 📞 测试接口示例

### 使用curl测试

```bash
# 1. 获取列表（全部分类）
curl http://localhost:8080/api/ceramic/list?current=1&size=9

# 2. 获取列表（只看历史分类）
curl http://localhost:8080/api/ceramic/list?category=history

# 3. 获取详情
curl http://localhost:8080/api/ceramic/1

# 4. 增加浏览量
curl -X POST http://localhost:8080/api/ceramic/1/view

# 5. 获取分类统计
curl http://localhost:8080/api/ceramic/categories
```

### 使用Postman测试

导入以下接口：
- GET: `http://localhost:8080/api/ceramic/list?current=1&size=9`
- GET: `http://localhost:8080/api/ceramic/1`
- POST: `http://localhost:8080/api/ceramic/1/view`

---

## 🎉 完成！

恭喜！陶瓷文化模块已经完整开发完成并可以正常使用了。

**已实现的功能：**
- ✅ 后端完整API（4个接口）
- ✅ 前端页面改造（使用真实数据）
- ✅ 测试数据（10条丰富内容）
- ✅ 浏览量统计
- ✅ 分类筛选
- ✅ 分页功能

**测试数据包含：**
- 2条陶瓷历史内容
- 2条制作工艺内容
- 2条陶瓷名家内容
- 2条文化传承内容
- 2条额外测试数据

现在可以开始使用了！ 🚀

