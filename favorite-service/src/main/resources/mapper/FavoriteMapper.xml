<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jingdezhen.tourism.favorite.mapper.FavoriteMapper">

    <select id="selectByUserAndProduct" resultType="com.jingdezhen.tourism.common.entity.Favorite">
        SELECT * 
        FROM favorite 
        WHERE user_id = #{userId} 
          AND product_id = #{productId} 
        LIMIT 1
    </select>

    <update id="updateDeletedStatus">
        UPDATE favorite 
        SET deleted = #{deleted}, 
            update_time = NOW() 
        WHERE id = #{id}
    </update>

</mapper>

